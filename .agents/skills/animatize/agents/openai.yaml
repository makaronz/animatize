interface:
  display_name: "Animatize"
  short_description: "Repo-specific patterns and workflows for animatize"
  default_prompt: "Use the animatize repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true